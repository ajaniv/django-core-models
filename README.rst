===========
core-models
===========

core-models is a simple Django app which defines often used models.

Detailed documentation is in the "docs" directory.

Quick start
-----------

1. Add "core-models" to your INSTALLED_APPS setting like this::

    INSTALLED_APPS = [
        ...
        'core-models',
    ]