# docker-compose mysql configuration file
version: '2'
services:
  db-mysql:
    restart: always
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
    # In a 'real' environment passwords will be defined in the environment
    # by only using the key
    - MYSQL_ROOT_PASSWORD=rootpassword
    - MYSQL_USER=mysql
    - MYSQL_PASSWORD=mysql
    - MYSQL_DATABASE=django_core_utils
    
  web-mysql:
    build: 
      context: .
      dockerfile: docker-mysql-dockerfile.yml
    command: docker/run.sh -w 5 -t mysql -d db-mysql
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db-mysql
    env_file: 
      - docker/env/common
      - docker/env/dev/mysql
      
  
